export const metadata = {
    title: {
      default: 'Login'
    },
    description: "Hello From Login",
  };
  
  export default function Login({children}) {
    return (
      <div>
        {children}
      </div>
    );
  }